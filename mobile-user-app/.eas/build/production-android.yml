build:
  name: Build mobile-user-app Android
  steps:
    - eas/checkout
    - run:
        name: Install dependencies
        command: npm install --legacy-peer-deps
    - eas/prebuild
    - eas/build
