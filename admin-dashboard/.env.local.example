# Para desarrollo local — copia este archivo como .env.local
# y ajusta la URL al puerto donde corre tu backend

# Backend local (transport-service)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Backend de producción (cuando esté desplegado)
# NEXT_PUBLIC_API_URL=https://api.goingec.com
