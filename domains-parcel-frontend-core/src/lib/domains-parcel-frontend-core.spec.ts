import { domainsParcelFrontendCore } from './domains-parcel-frontend-core';

describe('domainsParcelFrontendCore', () => {
  it('should work', () => {
    expect(domainsParcelFrontendCore()).toEqual('domains-parcel-frontend-core');
  });
});
