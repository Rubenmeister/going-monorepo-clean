# parcel

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build parcel` to build the library.

## Running unit tests

Run `nx test parcel` to execute the unit tests via [Jest](https://jestjs.io).
