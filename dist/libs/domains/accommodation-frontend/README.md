# accommodation-frontend

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build accommodation-frontend` to build the library.

## Running unit tests

Run `nx test accommodation-frontend` to execute the unit tests via [Jest](https://jestjs.io).
