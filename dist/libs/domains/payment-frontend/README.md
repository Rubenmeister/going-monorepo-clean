# payment-frontend

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build payment-frontend` to build the library.

## Running unit tests

Run `nx test payment-frontend` to execute the unit tests via [Jest](https://jestjs.io).
