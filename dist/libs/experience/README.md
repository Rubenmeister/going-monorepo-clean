# experience

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build experience` to build the library.

## Running unit tests

Run `nx test experience` to execute the unit tests via [Jest](https://jestjs.io).
