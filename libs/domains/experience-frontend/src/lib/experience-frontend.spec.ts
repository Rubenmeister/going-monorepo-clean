import { experienceFrontend } from './experience-frontend';

describe('experienceFrontend', () => {
  it('should work', () => {
    expect(experienceFrontend()).toEqual('experience-frontend');
  });
});
