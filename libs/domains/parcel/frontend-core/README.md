# domains-parcel-frontend-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domains-parcel-frontend-core` to build the library.

## Running unit tests

Run `nx test domains-parcel-frontend-core` to execute the unit tests via [Jest](https://jestjs.io).
