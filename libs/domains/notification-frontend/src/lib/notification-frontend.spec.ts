import { notificationFrontend } from './notification-frontend';

describe('notificationFrontend', () => {
  it('should work', () => {
    expect(notificationFrontend()).toEqual('notification-frontend');
  });
});
