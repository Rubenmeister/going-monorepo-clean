# booking-frontend

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build booking-frontend` to build the library.

## Running unit tests

Run `nx test booking-frontend` to execute the unit tests via [Jest](https://jestjs.io).
